## 2026-01-28 - Automatic Form Accessibility
**Learning:** Reusable form components often suffer from missing label-input associations because developers forget to manually pass and link IDs.
**Action:** Implement `useId` in base components (`Input`, `Select`) to automatically generate and link IDs, ensuring accessibility by default without adding friction to the developer experience.
